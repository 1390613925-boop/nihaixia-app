# 自动构建并发布 APK 到 GitHub Release
#
# 触发方式：
#   1. 推送以 "v" 开头的 tag（如 v1.11.9）→ 自动构建并发布
#   2. 在 Actions 页面手动触发（workflow_dispatch）
#
# 产物：通用版 app-release.apk + 按 ABI 拆分的三架构包（arm64-v8a / armeabi-v7a / x86_64），
#       共 4 个安装包一同发布到与 tag 同名的 GitHub Release（README 下载区四链均有效）。
#
# 可选：配置仓库 Secrets 后可产出正式签名 APK（否则回退 debug 签名）：
#   KEYSTORE_BASE64      : base64 后的 .jks 签名文件
#   KEYSTORE_PASSWORD    : keystore 密码
#   KEY_ALIAS            : 别名
#   KEY_PASSWORD         : 密钥密码
#   生成示例：base64 -w0 your.keystore.jks

name: Build APK & Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write   # 供 softprops/action-gh-release 创建 Release

concurrency:
  group: build-apk-${{ github.ref }}
  cancel-in-progress: false

env:
  FLUTTER_VERSION: '3.47.0'
  NDK_VERSION: '28.2.13676358'

jobs:
  build:
    name: Build release APK
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Set up Flutter ${{ env.FLUTTER_VERSION }}
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: 'stable'
          cache: true

      - name: Install NDK ${{ env.NDK_VERSION }}
        run: |
          SDKMGR="$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager"
          yes | "$SDKMGR" --licenses >/dev/null 2>&1 || true
          echo "y" | "$SDKMGR" "ndk;${{ env.NDK_VERSION }}"

      - name: Decode required release signing key
        env:
          KEYSTORE_BASE64: ${{ secrets.KEYSTORE_BASE64 }}
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
        run: |
          if [ -n "$KEYSTORE_BASE64" ] && [ -n "$KEYSTORE_PASSWORD" ] && [ -n "$KEY_ALIAS" ] && [ -n "$KEY_PASSWORD" ]; then
            echo "$KEYSTORE_BASE64" | base64 -d > android/keystore.jks
            # 注意：build.gradle.kts 中 storeFile 以 android/app/ 为基准解析，
            # 故此处必须写 ../keystore.jks（与本地 ../release-key.jks 同构）。
            cat > android/key.properties <<EOF
          storeFile=../keystore.jks
          storePassword=$KEYSTORE_PASSWORD
          keyAlias=$KEY_ALIAS
          keyPassword=$KEY_PASSWORD
          EOF
            echo "已写入签名配置（密钥内容不回显）"
            ls -l android/keystore.jks
            echo "key.properties 字段数：$(grep -c '=' android/key.properties)"
          else
            echo "缺少正式签名 Secrets；停止发布，避免产生不可覆盖安装的 debug 包。"
            exit 1
          fi

      - name: Verify encrypted distribution layer
        run: |
          test -f lib/services/license_service.dart
          test -f lib/screens/activation_screen.dart
          grep -q "1390613925-boop" lib/services/update_service.dart
          grep -q "岐黄经方" lib/main.dart
          grep -q "isMinifyEnabled = true" android/app/build.gradle.kts
          grep -q "isShrinkResources = true" android/app/build.gradle.kts

      - name: Cache Gradle dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Tune Gradle heap for CI
        run: |
          # 仓库的 android/gradle.properties 是为本机 OOM 环境（pagefile ~800MB）调的：
          # 1GB 堆 + 串行 GC + 关守护进程。在 CI（~7GB 内存）上会让 R8 混淆慢 10~30 倍。
          # 此处只覆盖 CI 检出的副本，不影响你本地构建。
          sed -i 's|^org.gradle.jvmargs=.*|org.gradle.jvmargs=-Xmx4g -XX:MaxMetaspaceSize=1g -XX:+UseG1GC -XX:+HeapDumpOnOutOfMemoryError|' android/gradle.properties
          sed -i 's|^org.gradle.daemon=.*|org.gradle.daemon=true|' android/gradle.properties
          echo "--- android/gradle.properties (CI override) ---"
          grep -E "org.gradle.jvmargs|org.gradle.daemon" android/gradle.properties

      - name: Flutter pub get
        run: flutter pub get

      - name: Test card-key and personal update feed
        run: flutter test test/license_service_test.dart test/services/update_service_test.dart

      - name: Build APK (universal)
        run: flutter build apk --release

      - name: Stage encrypted universal APK
        run: |
          mkdir -p release-artifacts
          cp build/app/outputs/flutter-apk/app-release.apk release-artifacts/app-release.apk

      - name: Build explicitly named unlocked APK
        run: flutter build apk --release --dart-define=LICENSE_GATE_ENABLED=false

      - name: Stage unlocked APK
        run: cp build/app/outputs/flutter-apk/app-release.apk release-artifacts/app-unlocked-release.apk

      - name: Build APK (split per ABI)
        run: flutter build apk --release --split-per-abi

      - name: Stage encrypted split APKs
        run: cp build/app/outputs/flutter-apk/app-*-release.apk release-artifacts/

      - name: Publish to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: ${{ github.ref_name }}
          tag_name: ${{ github.ref_name }}
          # 动态引用与 tag 同名（vX.Y.Z）的 release note，避免每次发版硬编码旧版本
          body_path: release_notes/${{ github.ref_name }}.md
          fail_on_unmatched_files: false
          files: |
            release-artifacts/*.apk
